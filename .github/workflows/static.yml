# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch<!DOCTYPE html>
<html lang="bn">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SCT HelpChain Global - Official</title>
    <style>
        body { font-family: 'SolaimanLipi', Arial, sans-serif; margin: 0; padding: 0; background-color: #f0f2f5; }
        .header { background: #2c3e50; color: white; padding: 40px 20px; text-align: center; box-shadow: 0 4px 10px rgba(0,0,0,0.1); }
        .container { max-width: 1200px; margin: 50px auto; padding: 20px; text-align: center; }
        .hero-title { font-size: 32px; color: #2ecc71; margin-bottom: 10px; }
        .security-badge { display: inline-block; padding: 10px 20px; background: #fff; border: 2px solid #2ecc71; border-radius: 50px; font-weight: bold; color: #27ae60; margin-top: 20px; }
    </style>
</head>
<body>

<div class="header">
    <h1>SCT HELPCHAIN INTERNATIONAL</h1>
    <p>হাবিবুর রহমান (অন্তর) সাহেবের অফিশিয়াল ডিজিটাল এক্সচেঞ্জ গেটওয়ে</p>
</div>

<div class="container">
    <h2 class="hero-title">SCT HelpChain এ স্বাগতম</h2>
    <p>আমরা নিয়ে এসেছি আধুনিক এআই ভিত্তিক ডিজিটাল সলিউশন।</p>
    
    <div class="security-badge">
        🛡️ Data Protection Registered: ZA832552 (Pay Set Ltd)
    </div>
</div>

<div id="ai-hub">
    <div id="ai-launcher" onclick="toggleChat()" style="position:fixed; bottom:30px; right:30px; background:#2ecc71; width:70px; height:70px; border-radius:50%; display:flex; align-items:center; justify-content:center; cursor:pointer; box-shadow:0 15px 35px rgba(0,0,0,0.3); z-index:10000; border:3px solid #fff;">
        <span style="font-size:35px;">🤖</span>
    </div>

    <div id="ai-chat-window" style="display:none; position:fixed; bottom:110px; right:20px; width:350px; height:450px; background:#fff; border-radius:15px; box-shadow:0 20px 60px rgba(0,0,0,0.3); z-index:10000; overflow:hidden; border:1px solid #ddd;">
        <div style="background:#2ecc71; color:white; padding:15px; text-align:center; font-weight:bold;">SCT AI Support Studio</div>
        <div id="chat-messages" style="height:310px; overflow-y:auto; padding:15px; background:#f9f9f9; font-size:14px; display:flex; flex-direction:column; gap:10px;">
            <div style="background:white; padding:10px; border-radius:10px; box-shadow:0 2px 5px rgba(0,0,0,0.05);">আসসালামু আলাইকুম! আমি SCT Global এআই। আমি আপনার সেবায় নিয়োজিত।</div>
        </div>
        <div style="padding:15px; display:flex; gap:10px; border-top:1px solid #eee;">
            <input type="text" id="ai-input" placeholder="আপনার প্রশ্নটি লিখুন..." style="flex:1; padding:10px; border:1px solid #ddd; border-radius:20px; outline:none;">
            <button onclick="sendMsg()" style="background:#2ecc71; color:white; border:none; width:40px; height:40px; border-radius:50%; cursor:pointer;">➔</button>
        </div>
    </div>
</div>

<script>
    function toggleChat() {
        var chat = document.getElementById('ai-chat-window');
        chat.style.display = (chat.style.display === 'none') ? 'block' : 'none';
    }

    async function sendMsg() {
        var input = document.getElementById('ai-input');
        var messages = document.getElementById('chat-messages');
        var text = input.value.trim();
        if(!text) return;

        messages.innerHTML += `<div style="align-self:flex-end; background:#2ecc71; color:white; padding:10px; border-radius:10px;">${text}</div>`;
        input.value = '';
        messages.scrollTop = messages.scrollHeight;

        try {
            const response = await fetch('https://api.openai.com/v1/chat/completions', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': 'Bearer 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'
                },
                body: JSON.stringify({
                    model: "gpt-3.5-turbo",
                    messages: [{role: "system", content: "You are the SCT AI Support."}, {role: "user", content: text}]
                })
            });
            const data = await response.json();
            messages.innerHTML += `<div style="align-self:flex-start; background:white; padding:10px; border-radius:10px; border:1px solid #eee;">${data.choices[0].message.content}</div>`;
            messages.scrollTop = messages.scrollHeight;
        } catch(e) {
            messages.innerHTML += `<div style="text-align:center; font-size:12px; color:gray;">SCT AI কানেক্ট হচ্ছে... একটু অপেক্ষা করুন।</div>`;
        }
    }
</script>
</body>
</html>

  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: 

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages<div id="sct-ai-master-container">
    <div id="ai-button" onclick="openSCTChat()" style="position:fixed; bottom:30px; right:30px; background:linear-gradient(135deg, #2ecc71, #27ae60); color:white; width:70px; height:70px; border-radius:50%; display:flex; align-items:center; justify-content:center; cursor:pointer; box-shadow:0 10px 30px rgba(0,0,0,0.4); z-index:1000000; border:2px solid #fff;">
        <span style="font-size:35px;">🤖</span>
    </div>

    <div id="sct-chat-window" style="display:none; position:fixed; bottom:110px; right:25px; width:350px; background:white; border-radius:20px; box-shadow:0 20px 50px rgba(0,0,0,0.3); z-index:1000000; overflow:hidden; border:1px solid #eee; font-family: 'SolaimanLipi', Arial, sans-serif;">
        <div style="background:#2ecc71; padding:20px; color:white; text-align:center;">
            <strong style="font-size:18px;">SCT HelpChain AI Support</strong>
            <p style="font-size:12px; margin:5px 0 0; opacity:0.9;">মুহতামিম হাবিবুর রহমান (অন্তর) সাহেবের তত্ত্বাবধানে</p>
        </div>
        <div id="sct-messages" style="height:350px; overflow-y:auto; padding:15px; background:#f9f9f9; display:flex; flex-direction:column; gap:10px;">
            <div style="background:white; padding:10px; border-radius:10px; font-size:14px; box-shadow:0 2px 5px rgba(0,0,0,0.05);">আসসালামু আলাইকুম! আমি আপনার ইন্টারন্যাশনাল ডিজিটাল গেটওয়ে এআই। আপনাকে কীভাবে সাহায্য করতে পারি?</div>
        </div>
        <div style="padding:15px; display:flex; gap:10px; border-top:1px solid #eee;">
            <input type="text" id="sct-input" placeholder="আপনার প্রশ্ন লিখুন..." style="flex:1; padding:12px; border:1px solid #ddd; border-radius:25px; outline:none;">
            <button onclick="sendSCTMessage()" style="background:#2ecc71; color:white; border:none; width:45px; height:45px; border-radius:50%; cursor:pointer; font-weight:bold;">➔</button>
        </div>
    </div>
</div>

<script>
    function openSCTChat() {
        var win = document.getElementById('sct-chat-window');
        win.style.display = (win.style.display === 'none') ? 'block' : 'none';
    }

    async function sendSCTMessage() {
        var input = document.getElementById('sct-input');
        var area = document.getElementById('sct-messages');
        var text = input.value.trim();
        if(!text) return;

        area.innerHTML += `<div style="align-self:flex-end; background:#2ecc71; color:white; padding:10px 15px; border-radius:15px; max-width:80%; font-size:14px;">${text}</div>`;
        input.value = '';
        area.scrollTop = area.scrollHeight;

        try {
            const response = await fetch('https://api.openai.com/v1/chat/completions', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': 'Bearer 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'
                },
                body: JSON.stringify({
                    model: "gpt-3.5-turbo",
                    messages: [{role: "system", content: "You are the Official AI for SCT HelpChain. You assist users with digital exchange and security. Respond in Bengali."}, {role: "user", content: text}]
                })
            });
            const data = await response.json();
            const reply = data.choices[0].message.content;
            area.innerHTML += `<div style="align-self:flex-start; background:white; padding:10px 15px; border-radius:15px; box-shadow:0 2px 5px rgba(0,0,0,0.1); max-width:80%; font-size:14px;">${reply}</div>`;
            area.scrollTop = area.scrollHeight;
        } catch (e) {
            area.innerHTML += `<div style="text-align:center; font-size:12px; color:gray;">সার্ভার কানেক্ট হচ্ছে... কিছুক্ষণ পর আবার লিখুন।</div>`;
        }
    }
</script>

        id: deployment
        uses: actions/deploy-pages@v5<div id="sct-ai-service">
    <div id="sct-launcher" onclick="toggleSCTChat()" style="position:fixed; bottom:30px; right:30px; background:linear-gradient(135deg, #2ecc71, #27ae60); color:white; width:65px; height:65px; border-radius:50%; display:flex; align-items:center; justify-content:center; cursor:pointer; box-shadow:0 10px 25px rgba(0,0,0,0.3); z-index:999999; border:2px solid #fff; transition: 0.3s;">
        <span style="font-size:30px;">🤖</span>
    </div>

    <div id="sct-chat-panel" style="display:none; position:fixed; bottom:105px; right:25px; width:360px; height:500px; background:white; border-radius:20px; box-shadow:0 15px 50px rgba(0,0,0,0.2); z-index:999999; overflow:hidden; border:1px solid #ddd; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;">
        <div style="background:#2ecc71; padding:20px; color:white; display:flex; align-items:center; gap:12px;">
            <div style="background:white; color:#2ecc71; width:40px; height:40px; border-radius:50%; display:flex; align-items:center; justify-content:center; font-weight:bold; font-size:20px;">S</div>
            <div>
                <div style="font-weight:bold; font-size:16px;">SCT Global AI Studio</div>
                <div style="font-size:11px; opacity:0.9;">মুহতামিম হাবিবুর রহমান (অন্তর) সাহেবের অফিশিয়াল এআই</div>
            </div>
        </div>

        <div id="sct-chat-messages" style="height:340px; overflow-y:auto; padding:15px; background:#f4f7f6; display:flex; flex-direction:column; gap:12px;">
            <div style="background:white; padding:12px; border-radius:15px; border-bottom-left-radius:2px; box-shadow:0 2px 5px rgba(0,0,0,0.05); font-size:14px; color:#444; line-height:1.5;">
                আসসালামু আলাইকুম মুহতামিম সাহেব! আমি আপনার ব্যক্তিগত এআই অ্যাসিস্ট্যান্ট। আমি আপনাকে এবং আপনার ইউজারদের সাহায্য করার জন্য প্রস্তুত।
            </div>
        </div>

        <div style="padding:15px; background:white; border-top:1px solid #eee; display:flex; gap:10px;">
            <input type="text" id="sct-user-input" placeholder="আপনার প্রশ্নটি লিখুন..." style="flex:1; padding:12px 18px; border:1px solid #e0e0e0; border-radius:30px; outline:none; font-size:14px;">
            <button onclick="askSCTAI()" style="background:#2ecc71; color:white; border:none; width:45px; height:45px; border-radius:50%; cursor:pointer; font-weight:bold; font-size:18px; transition:0.2s;">➤</button>
        </div>
    </div>
</div>

<script>
    function toggleSCTChat() {
        const panel = document.getElementById('sct-chat-panel');
        panel.style.display = (panel.style.display === 'none') ? 'block' : 'none';
    }

    async function askSCTAI() {
        const input = document.getElementById('sct-user-input');
        const box = document.getElementById('sct-chat-messages');
        const userText = input.value.trim();
        
        if (!userText) return;

        // ইউজারের মেসেজ যোগ করা
        box.innerHTML += `<div style="align-self:flex-end; background:#2ecc71; color:white; padding:10px 15px; border-radius:15px; border-bottom-right-radius:2px; max-width:80%; font-size:14px;">${userText}</div>`;
        input.value = '';
        box.scrollTop = box.scrollHeight;

        try {
            const response = await fetch('https://api.openai.com/v1/chat/completions', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'Authorization': 'Bearer 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'
                },
                body: JSON.stringify({
                    model: "gpt-3.5-turbo",
                    messages: [
                        {role: "system", content: "You are a professional assistant for SCT Global. Be respectful and respond in Bengali."},
                        {role: "user", content: userText}
                    ]
                })
            });

            const data = await response.json();
            const aiReply = data.choices[0].message.content;

            // এআই এর উত্তর যোগ করা
            box.innerHTML += `<div style="align-self:flex-start; background:white; padding:12px; border-radius:15px; border-bottom-left-radius:2px; box-shadow:0 2px 5px rgba(0,0,0,0.1); max-width:80%; font-size:14px; color:#333;">${aiReply}</div>`;
            box.scrollTop = box.scrollHeight;
        } catch (error) {
            box.innerHTML += `<div style="text-align:center; font-size:12px; color:red; margin:10px 0;">সার্ভার একটু বিজি, দয়া করে ১ মিনিট পর আবার চেষ্টা করুন।</div>`;
        }
    }
</script>

